# VALIDATION

* for validation from the *JSON*, identify the *record* with the `PAYORCONTROLNUMBER` and match the rest of the attributes like:

* *ACTION*
* *CHECKAMOUNT*
* etc

to confirm the *record*
